# SECURITY: Exact versions on install/save — no ^ or ~ ranges.
save-exact=true

# SECURITY: Explicit registry to prevent registry confusion attacks.
registry=https://registry.npmjs.org/

# SECURITY: Reject any package version published less than 24h ago.
min-release-age=1
